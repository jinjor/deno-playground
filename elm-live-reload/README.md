# Elm Live Reload

```
deno https://raw.githubusercontent.com/jinjor/deno-playground/master/elm-live-reload/cli.ts src/Main.elm src/index.html --allow-net --allow-run
```

```
deno ../cli.ts src/Main.elm src/index.html --allow-net --allow-run
```
